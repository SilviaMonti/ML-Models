# ML-Models
This is a repository with some machine learning models codes in python.

Every sub-branch into the main one refers to a specific project, containing the code, the dataset and some possible notes.
